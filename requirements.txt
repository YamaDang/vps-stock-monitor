# 核心依赖（严格匹配，确保无兼容问题）
flask==2.0.1
flask-login==0.5.0
flask-sqlalchemy==2.5.1  # 依赖 sqlalchemy 1.x
sqlalchemy==1.4.46       # 与 flask-sqlalchemy 2.5.1 严格兼容，保留 __all__ 属性
werkzeug==2.0.1
gunicorn==20.1.0

# 其他依赖
requests==2.26.0
beautifulsoup4==4.10.0
python-dotenv==0.19.2
